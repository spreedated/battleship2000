<Page x:Class="Battleship2000.Views.Pages.Settings_Visual"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Battleship2000.Views.Pages"
      xmlns:vm="clr-namespace:Battleship2000.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Settings_Visual">
    <Page.DataContext>
        <vm:SettingsVisualViewModel />
    </Page.DataContext>
    <StackPanel Margin="10,10,10,10">
        <StackPanel Orientation="Vertical">
            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Background:" VerticalAlignment="Center" HorizontalAlignment="Left" TextAlignment="Center" Style="{StaticResource TextHeadline}" FontSize="18"/>
                <ComboBox x:Name="CMB_Background" Grid.Column="1" HorizontalAlignment="Left" MinWidth="180" ItemsSource="{Binding Backgrounds}" SelectedItem="{Binding SelectedBackground}" DisplayMemberPath="Name" />
            </Grid>
        </StackPanel>
    </StackPanel>
</Page>
