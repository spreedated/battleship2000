<Page x:Class="Battleship2000.Views.Pages.Settings_Player"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Battleship2000.Views.Pages"
      xmlns:vm="clr-namespace:Battleship2000.ViewModels"
      xmlns:logic="clr-namespace:Battleship2000.Logic"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Settings_Player" Loaded="Page_Loaded">
    <Page.DataContext>
        <vm:SettingsPlayerViewModel/>
    </Page.DataContext>
    <StackPanel Margin="10,10,10,10">
        <StackPanel Orientation="Vertical">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Playername:" VerticalAlignment="Center" HorizontalAlignment="Left" TextAlignment="Center" Style="{StaticResource TextHeadline}" FontSize="18"/>
                <TextBox x:Name="TXT_Playername" Grid.Column="1" HorizontalAlignment="Left" MinWidth="180" Style="{StaticResource TextBoxStyle}" Text="{Binding Playername}" />
            </Grid>
        </StackPanel>
    </StackPanel>
</Page>
